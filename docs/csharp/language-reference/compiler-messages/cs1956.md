---
description: Ostrzeżenie kompilatora (poziom 1) CS1956
title: Ostrzeżenie kompilatora (poziom 1) CS1956
ms.date: 07/20/2015
f1_keywords:
- CS1956
helpviewer_keywords:
- CS1956
ms.assetid: 841f8b2b-692b-49ae-a587-a40797cf9797
ms.openlocfilehash: b59eb0b351f5e75feae270bf39c552d62d9dabae
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/30/2020
ms.locfileid: "89126271"
---
# <a name="compiler-warning-level-1-cs1956"></a><span data-ttu-id="21814-103">Ostrzeżenie kompilatora (poziom 1) CS1956</span><span class="sxs-lookup"><span data-stu-id="21814-103">Compiler Warning (level 1) CS1956</span></span>
<span data-ttu-id="21814-104">Składowa "name" implementuje składową interfejsu "name" w typie "Type".</span><span class="sxs-lookup"><span data-stu-id="21814-104">Member 'name' implements interface member 'name' in type 'type'.</span></span> <span data-ttu-id="21814-105">Istnieje wiele dopasowań dla elementu członkowskiego interfejsu w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="21814-105">There are multiple matches for the interface member at run-time.</span></span> <span data-ttu-id="21814-106">Zależy od implementacji, która metoda zostanie wywołana.</span><span class="sxs-lookup"><span data-stu-id="21814-106">It is implementation dependent which method will be called.</span></span>  
  
 <span data-ttu-id="21814-107">To ostrzeżenie można wygenerować, gdy dwie metody interfejsu są rozróżniane tylko wtedy, gdy określony parametr jest oznaczony za pomocą `ref` lub z `out` .</span><span class="sxs-lookup"><span data-stu-id="21814-107">This warning can be generated when two interface methods are differentiated only by whether a particular parameter is marked with `ref` or with `out`.</span></span> <span data-ttu-id="21814-108">Najlepiej zmienić swój kod, aby uniknąć tego ostrzeżenia, ponieważ nie jest to oczywiste ani gwarantowane, która metoda jest wywoływana w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="21814-108">It is best to change your code to avoid this warning because it is not obvious or guaranteed which method is called at runtime.</span></span>  
  
 <span data-ttu-id="21814-109">Chociaż C# rozróżnia między `out` i `ref` , środowisko CLR widzi je jako takie same.</span><span class="sxs-lookup"><span data-stu-id="21814-109">Although C# distinguishes between `out` and `ref`, the CLR sees them as the same.</span></span> <span data-ttu-id="21814-110">Podczas wybierania metody implementującej interfejs środowisko CLR po prostu wybiera je.</span><span class="sxs-lookup"><span data-stu-id="21814-110">When deciding which method implements the interface, the CLR just picks one.</span></span>  
  
### <a name="to-avoid-this-warning"></a><span data-ttu-id="21814-111">Aby uniknąć tego ostrzeżenia</span><span class="sxs-lookup"><span data-stu-id="21814-111">To avoid this warning</span></span>  
  
1. <span data-ttu-id="21814-112">Zapewnij kompilatorowi jakiś sposób odróżnienia metod.</span><span class="sxs-lookup"><span data-stu-id="21814-112">Give the compiler some way to differentiate the methods.</span></span> <span data-ttu-id="21814-113">Na przykład możesz nadać im różne nazwy lub podać dodatkowy parametr na jednym z nich.</span><span class="sxs-lookup"><span data-stu-id="21814-113">For example, you can give them different names or provide an additional parameter on one of them.</span></span>  
  
## <a name="example"></a><span data-ttu-id="21814-114">Przykład</span><span class="sxs-lookup"><span data-stu-id="21814-114">Example</span></span>  
 <span data-ttu-id="21814-115">Poniższy kod generuje CS1956, ponieważ dwie `Test` metody `Base` różnią się tylko `ref` / `out` modyfikatorem pierwszego parametru.</span><span class="sxs-lookup"><span data-stu-id="21814-115">The following code generates CS1956 because the two `Test` methods in `Base` differ only by the `ref`/`out` modifier on the first parameter.</span></span>  
  
```csharp  
// cs1956.cs  
class Base<T, S>  
{  
    // This is the method that should be called.  
    public virtual int Test(out T x) // CS1956  
    {  
        x = default(T);  
        return 0;  
    }  
  
    // This is the "last" method and is the one that ends up being called  
    public virtual int Test(ref S x)  
    {  
        return 1;  
    }  
}  
  
interface IFace  
{  
    int Test(out int x);  
}  
  
class Derived : Base<int, int>, IFace  
{  
    static int Main()  
    {  
        IFace x = new Derived();  
        int y;  
        return x.Test(out y);  
    }  
}  
```
