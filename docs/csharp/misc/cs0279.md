---
description: Ostrzeżenie kompilatora (poziom 2) CS0279
title: Ostrzeżenie kompilatora (poziom 2) CS0279
ms.date: 07/20/2015
f1_keywords:
- CS0279
helpviewer_keywords:
- CS0279
ms.assetid: 5e5faa8f-3d5b-4999-aa62-ff7f131a3e04
ms.openlocfilehash: d9109d0b8cd95728ef2ebcf27d068300a22519b0
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/30/2020
ms.locfileid: "89143262"
---
# <a name="compiler-warning-level-2-cs0279"></a><span data-ttu-id="30190-103">Ostrzeżenie kompilatora (poziom 2) CS0279</span><span class="sxs-lookup"><span data-stu-id="30190-103">Compiler Warning (level 2) CS0279</span></span>
<span data-ttu-id="30190-104">element "type name" nie implementuje wzorca "name pattern".</span><span class="sxs-lookup"><span data-stu-id="30190-104">'type name' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="30190-105">"metoda name" jest statyczna lub nie jest publiczna.</span><span class="sxs-lookup"><span data-stu-id="30190-105">'method name' is either static or not public.</span></span>  
  
 <span data-ttu-id="30190-106">W języku C# istnieje kilka instrukcji, które opierają się na zdefiniowanych wzorcach, takich jak `foreach` i `using` .</span><span class="sxs-lookup"><span data-stu-id="30190-106">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="30190-107">Na przykład, `foreach` bazuje na klasie kolekcji implementującej wyliczalny wzorzec.</span><span class="sxs-lookup"><span data-stu-id="30190-107">For example, `foreach` relies on the collection class implementing the enumerable pattern.</span></span> <span data-ttu-id="30190-108">Ten błąd występuje, gdy kompilator nie może wykonać dopasowania z powodu zadeklarowanej metody `static` `public` .</span><span class="sxs-lookup"><span data-stu-id="30190-108">This error occurs when the compiler is unable to make the match due to a method being declared `static` or not `public`.</span></span> <span data-ttu-id="30190-109">Metody we wzorcach muszą być wystąpieniami klas i być publiczne.</span><span class="sxs-lookup"><span data-stu-id="30190-109">Methods in patterns are required to be instances of classes, and to be public.</span></span>  
  
## <a name="example"></a><span data-ttu-id="30190-110">Przykład</span><span class="sxs-lookup"><span data-stu-id="30190-110">Example</span></span>  
 <span data-ttu-id="30190-111">Poniższy przykład generuje CS0279:</span><span class="sxs-lookup"><span data-stu-id="30190-111">The following example generates CS0279:</span></span>  
  
```csharp  
// CS0279.cs  
  
using System;  
using System.Collections;  
  
public class myTest : IEnumerable  
{  
    IEnumerator IEnumerable.GetEnumerator()  
    {  
        yield return 0;  
    }  
  
    internal IEnumerator GetEnumerator()  
    {  
        yield return 0;  
    }  
  
    public static void Main()  
    {  
        foreach (int i in new myTest()) {}  // CS0279  
    }  
}  
```
