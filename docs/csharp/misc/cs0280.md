---
description: Ostrzeżenie kompilatora (poziom 2) CS0280
title: Ostrzeżenie kompilatora (poziom 2) CS0280
ms.date: 07/20/2015
f1_keywords:
- CS0280
helpviewer_keywords:
- CS0280
ms.assetid: 9b453478-92aa-4fd2-9b87-780fd138603a
ms.openlocfilehash: 658164f0ed05a54a9a9292e6b205a1d5179b987d
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/24/2020
ms.locfileid: "91174813"
---
# <a name="compiler-warning-level-2-cs0280"></a><span data-ttu-id="82183-103">Ostrzeżenie kompilatora (poziom 2) CS0280</span><span class="sxs-lookup"><span data-stu-id="82183-103">Compiler Warning (level 2) CS0280</span></span>

<span data-ttu-id="82183-104">element "Type" nie implementuje wzorca "name" wzorca.</span><span class="sxs-lookup"><span data-stu-id="82183-104">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="82183-105">"Nazwa metody" ma niewłaściwą sygnaturę.</span><span class="sxs-lookup"><span data-stu-id="82183-105">'method name' has the wrong signature.</span></span>  
  
 <span data-ttu-id="82183-106">Dwie instrukcje w języku C#, **foreach** i **using używają**odpowiednio wstępnie zdefiniowanych wzorców, "kolekcji" i "Resource".</span><span class="sxs-lookup"><span data-stu-id="82183-106">Two statements in C#, **foreach** and **using**, rely on predefined patterns, "collection" and "resource" respectively.</span></span> <span data-ttu-id="82183-107">To ostrzeżenie występuje, gdy kompilator nie może dopasować jednej z tych instrukcji do jego wzorca ze względu na niepoprawną sygnaturę metody.</span><span class="sxs-lookup"><span data-stu-id="82183-107">This warning occurs when the compiler cannot match one of these statements to its pattern due to a method's incorrect signature.</span></span> <span data-ttu-id="82183-108">Na przykład wzorzec "Collection" wymaga, aby była wywoływana metoda, <xref:System.Collections.IEnumerator.MoveNext%2A> która nie przyjmuje parametrów i zwraca wartość `boolean` .</span><span class="sxs-lookup"><span data-stu-id="82183-108">For example, the "collection" pattern requires that there be a method called <xref:System.Collections.IEnumerator.MoveNext%2A> which takes no parameters and returns a `boolean`.</span></span> <span data-ttu-id="82183-109">Kod może zawierać <xref:System.Collections.IEnumerator.MoveNext%2A> metodę, która ma parametr lub prawdopodobnie zwraca obiekt.</span><span class="sxs-lookup"><span data-stu-id="82183-109">Your code might contain a <xref:System.Collections.IEnumerator.MoveNext%2A> method that has a parameter or perhaps returns an object.</span></span>  
  
 <span data-ttu-id="82183-110">Wzorzec "Resource" i `using` dostarczenie innego przykładu.</span><span class="sxs-lookup"><span data-stu-id="82183-110">The "resource" pattern and `using` provide another example.</span></span> <span data-ttu-id="82183-111">Wzorzec "Resource" wymaga <xref:System.IDisposable.Dispose%2A> metody; jeśli zdefiniujesz właściwość o tej samej nazwie, zostanie wyświetlone ostrzeżenie.</span><span class="sxs-lookup"><span data-stu-id="82183-111">The "resource" pattern requires the <xref:System.IDisposable.Dispose%2A> method; if you define a property with the same name, you will get this warning.</span></span>  
  
 <span data-ttu-id="82183-112">Aby rozwiązać ten problem, upewnij się, że sygnatury metod w danym typie pasują do sygnatur odpowiednich metod we wzorcu i upewnij się, że nie masz żadnych właściwości o tej samej nazwie co Metoda wymagana przez wzorzec.</span><span class="sxs-lookup"><span data-stu-id="82183-112">To resolve this warning, ensure that the method signatures in your type match the signatures of the corresponding methods in the pattern, and ensure that you have no properties with the same name as a method required by the pattern.</span></span>  
  
## <a name="example"></a><span data-ttu-id="82183-113">Przykład</span><span class="sxs-lookup"><span data-stu-id="82183-113">Example</span></span>  

 <span data-ttu-id="82183-114">Poniższy przykład generuje CS0280.</span><span class="sxs-lookup"><span data-stu-id="82183-114">The following sample generates CS0280.</span></span>  
  
```csharp  
// CS0280.cs  
using System;  
using System.Collections;  
  
public class ValidBase: IEnumerable  
{  
   IEnumerator IEnumerable.GetEnumerator()  
   {  
      yield return 0;  
   }  
  
   internal IEnumerator GetEnumerator()  
   {  
      yield return 0;  
   }  
}  
  
class Derived : ValidBase  
{  
   // field, not method  
   new public int GetEnumerator;  
}  
  
public class Test  
{  
   public static void Main()  
   {  
      foreach (int i in new Derived()) {}   // CS0280  
   }  
}  
```
