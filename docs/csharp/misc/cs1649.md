---
description: Błąd kompilatora CS1649
title: Błąd kompilatora CS1649
ms.date: 07/20/2015
f1_keywords:
- CS1649
helpviewer_keywords:
- CS1649
ms.assetid: 6355c7f2-157c-441d-8925-500062988636
ms.openlocfilehash: 50479437f6afa7d317cd13184f56e0df2757a8db
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/24/2020
ms.locfileid: "91170873"
---
# <a name="compiler-error-cs1649"></a><span data-ttu-id="a5e13-103">Błąd kompilatora CS1649</span><span class="sxs-lookup"><span data-stu-id="a5e13-103">Compiler Error CS1649</span></span>

<span data-ttu-id="a5e13-104">Składowych pola tylko do odczytu nie można przekazywać ref ani out (z wyjątkiem konstruktora)</span><span class="sxs-lookup"><span data-stu-id="a5e13-104">Members of readonly field 'identifier' cannot be passed ref or out (except in a constructor)</span></span>  
  
 <span data-ttu-id="a5e13-105">Ten błąd występuje w przypadku przekazania zmiennej do funkcji, która jest elementem członkowskim `readonly` pola jako `ref` `out` argumentu lub.</span><span class="sxs-lookup"><span data-stu-id="a5e13-105">This error occurs if you pass a variable to a function that is a member of a `readonly` field as a `ref` or `out` argument.</span></span> <span data-ttu-id="a5e13-106">Ponieważ `ref` `out` Parametry i mogą być modyfikowane przez funkcję, nie jest to dozwolone.</span><span class="sxs-lookup"><span data-stu-id="a5e13-106">Since `ref` and `out` parameters may be modified by the function, this is not allowed.</span></span> <span data-ttu-id="a5e13-107">Aby rozwiązać ten problem, Usuń `readonly` słowo kluczowe w polu lub nie przekazuj elementów członkowskich `readonly` pola do funkcji.</span><span class="sxs-lookup"><span data-stu-id="a5e13-107">To resolve this error, remove the `readonly` keyword on the field, or do not pass the members of the `readonly` field to the function.</span></span> <span data-ttu-id="a5e13-108">Na przykład możesz spróbować utworzyć zmienną tymczasową, która może być modyfikowana i przekazywać tymczasowy jako `ref` argument, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="a5e13-108">For example, you might try creating a temporary variable which can be modified and passing the temporary as a `ref` argument, as shown in the following example.</span></span>  
  
## <a name="example"></a><span data-ttu-id="a5e13-109">Przykład</span><span class="sxs-lookup"><span data-stu-id="a5e13-109">Example</span></span>  

 <span data-ttu-id="a5e13-110">Poniższy przykład generuje CS1649:</span><span class="sxs-lookup"><span data-stu-id="a5e13-110">The following sample generates CS1649:</span></span>  
  
```csharp  
// CS1649.cs  
public struct Inner  
    {  
        public int i;  
    }  
  
class Outer  
{  
    public readonly Inner inner = new Inner();  
}  
  
class D  
{  
    static void f(ref int iref)  
    {  
    }  
  
    static void Main()  
    {  
        Outer outer = new Outer();
        f(ref outer.inner.i);  // CS1649  
        // Try this code instead:  
        // int tmp = outer.inner.i;  
        // f(ref tmp);  
    }  
}  
```
