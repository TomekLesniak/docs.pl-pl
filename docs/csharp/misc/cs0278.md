---
description: Ostrzeżenie kompilatora (poziom 2) CS0278
title: Ostrzeżenie kompilatora (poziom 2) CS0278
ms.date: 07/20/2015
f1_keywords:
- CS0278
helpviewer_keywords:
- CS0278
ms.assetid: 5418cbbe-bcec-4379-a6f6-410987beb96a
ms.openlocfilehash: 4536b143a39f5640dec9b2a5a4a781d810b28fb2
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/24/2020
ms.locfileid: "91177205"
---
# <a name="compiler-warning-level-2-cs0278"></a><span data-ttu-id="625a7-103">Ostrzeżenie kompilatora (poziom 2) CS0278</span><span class="sxs-lookup"><span data-stu-id="625a7-103">Compiler Warning (level 2) CS0278</span></span>

<span data-ttu-id="625a7-104">element "Type" nie implementuje wzorca "name" wzorca.</span><span class="sxs-lookup"><span data-stu-id="625a7-104">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="625a7-105">Metoda "name" jest niejednoznaczna przy użyciu metody "name".</span><span class="sxs-lookup"><span data-stu-id="625a7-105">'method name' is ambiguous with 'method name'.</span></span>  
  
<span data-ttu-id="625a7-106">W języku C# istnieje kilka instrukcji, które opierają się na zdefiniowanych wzorcach, takich jak `foreach` i `using` .</span><span class="sxs-lookup"><span data-stu-id="625a7-106">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="625a7-107">Na przykład [ `foreach` instrukcja](../language-reference/keywords/foreach-in.md) opiera się na klasie kolekcji implementującej wzorzec "wyliczalne".</span><span class="sxs-lookup"><span data-stu-id="625a7-107">For example, the [`foreach` statement](../language-reference/keywords/foreach-in.md) relies on the collection class implementing the "enumerable" pattern.</span></span>
  
<span data-ttu-id="625a7-108">CS0278 może wystąpić, jeśli kompilator nie może wykonać dopasowania ze względu na niejasności.</span><span class="sxs-lookup"><span data-stu-id="625a7-108">CS0278 can occur if the compiler is unable to make the match due to ambiguities.</span></span> <span data-ttu-id="625a7-109">Na przykład wzorzec "wyliczalny" wymaga wywołania metody `MoveNext` , a kod może zawierać dwie metody o nazwie `MoveNext` .</span><span class="sxs-lookup"><span data-stu-id="625a7-109">For example, the "enumerable" pattern requires that there be a method called `MoveNext`, and your code might contain two methods called `MoveNext`.</span></span> <span data-ttu-id="625a7-110">Kompilator podejmie próbę znalezienia interfejsu, który ma być używany, ale zaleca się określenie i rozwiązanie przyczyny niejednoznaczności.</span><span class="sxs-lookup"><span data-stu-id="625a7-110">The compiler will attempt to find an interface to use, but it is recommended that you determine and resolve the cause of the ambiguity.</span></span>  
  
## <a name="example"></a><span data-ttu-id="625a7-111">Przykład</span><span class="sxs-lookup"><span data-stu-id="625a7-111">Example</span></span>  

 <span data-ttu-id="625a7-112">Poniższy przykład generuje CS0278.</span><span class="sxs-lookup"><span data-stu-id="625a7-112">The following sample generates CS0278.</span></span>  
  
```csharp  
// CS0278.cs  
using System.Collections.Generic;  
public class myTest
{  
   public static void TestForeach<W>(W w)
      where W: IEnumerable<int>, IEnumerable<string>  
   {  
      foreach (int i in w) {}   // CS0278  
   }  
}  
```
