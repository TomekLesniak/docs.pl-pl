---
title: "\"#Else\" musi być poprzedzona odpowiadającą jej instrukcją \"#If\" lub \"#ElseIf\""
ms.date: 07/20/2015
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
ms.openlocfilehash: af07cde04fee9f0e07988525b935517ce1cbb62b
ms.sourcegitcommit: bf5c5850654187705bc94cc40ebfb62fe346ab02
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/23/2020
ms.locfileid: "91086923"
---
# <a name="else-must-be-preceded-by-a-matching-if-or-elseif"></a><span data-ttu-id="c803e-102">"#Else" musi być poprzedzona odpowiadającą jej instrukcją "#If" lub "#ElseIf"</span><span class="sxs-lookup"><span data-stu-id="c803e-102">'#Else' must be preceded by a matching '#If' or '#ElseIf'</span></span>

<span data-ttu-id="c803e-103">`#Else` jest dyrektywy kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="c803e-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="c803e-104">`#Else`Dyrektywa nie jest poprzedzona odpowiadającą `#If` lub `#ElseIf` dyrektywą.</span><span class="sxs-lookup"><span data-stu-id="c803e-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="c803e-105">**Identyfikator błędu:** BC30028</span><span class="sxs-lookup"><span data-stu-id="c803e-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="c803e-106">Aby poprawić ten błąd</span><span class="sxs-lookup"><span data-stu-id="c803e-106">To correct this error</span></span>  
  
1. <span data-ttu-id="c803e-107">Sprawdź, czy poprzedzająca `#If` lub `#ElseIf` nie jest oddzielona od niego `#Else` przez interwencję warunkowego lub nieprawidłowo umieszczonym `#End If` .</span><span class="sxs-lookup"><span data-stu-id="c803e-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2. <span data-ttu-id="c803e-108">Sprawdź, czy `#Else` jest poprzedzana przez inną `#Else` dyrektywę.</span><span class="sxs-lookup"><span data-stu-id="c803e-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="c803e-109">Jeśli tak, Usuń `#Else` lub zmień go na `#ElseIf` .</span><span class="sxs-lookup"><span data-stu-id="c803e-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3. <span data-ttu-id="c803e-110">Jeśli wszystko inne jest w porządku, Dodaj `#If` dyrektywę na początku bloku kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="c803e-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c803e-111">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="c803e-111">See also</span></span>

- [<span data-ttu-id="c803e-112">#If... Then... #Else — dyrektywy</span><span class="sxs-lookup"><span data-stu-id="c803e-112">#If...Then...#Else Directives</span></span>](../language-reference/directives/if-then-else-directives.md)
